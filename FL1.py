variable = 0
#Variable is set to 0, this means that it will increase by one each time
for variable in range (0,100): # Within 100 tries the code below is going to happen
    variable = variable + 1 # The variable is adding one to show it going up
    print (variable) # This is now printing the new variable
