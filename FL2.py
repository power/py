name = input("What is your name?") #This is asking the user for their name and then saving it as the variable called "name"

for counter in range (10): # This is printing the code below 10 times, you can define the amount of times within the brackets
    print (name) # Printing the variable name
