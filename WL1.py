number = 0
# On the first line we are adding a variable called number which is defined as the number 0.
while number < 20:
    number = number + 1
    print (number)
# Above this is some code which will define the number's new variable.
#On the first line it says that whilst the variable number is below 20, followed by the next line saying that whilst number is below 20 is to add 1 to the number variable.
#On the bottom line the code is printing what the new variable will be before repeating it.
