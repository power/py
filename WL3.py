variable = float(input("Pick a number between 1 and 10"))# We are asking them for a numerical variable which will be saved
while variable >= 10:# If the number is above 10 then the user will be asked to input another number
    variable = float(input("Pick a number between 1 and 10"))# This is the same as line one as we are asking them to redefine the variable
print ("That is between 1 and 10")# This is what will happen if the variable is within 1 - 10
